#include <iostream>
using namespace std;
int main () {
    int arr [5] = {1,2,3,4,5};
    cout << arr [3] << endl;
}

// No need to write size if array is declared and initialized
// at the same time. In all other cases size needs to be written.